[[source]]
url = "https://pypi.org/simple"
verify_ssl = true
name = "pypi"

[packages]
Django = ">=4.2,<5.1"
openpyxl = ">=3.1"
python-decouple = ">=3.8"
Pillow = ">=10.0"
firebase-admin = ">=6.0"
google-cloud-firestore = ">=2.27.0"

[dev-packages]

[requires]
python_version = "3.9"
